{
  "name": "github-file-patch-api",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "GitHub control plane with REST/GraphQL, App/installation auth, repository diagnostics, CI logs, scoped write probes, and safe patching.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/server.mjs && node --check src/github.mjs && node --check src/openapi.mjs && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1",
    "diff": "^7.0.0"
  },
  "devDependencies": {}
}
